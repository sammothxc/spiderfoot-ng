# Linting tools, run by the CI "lint" job. Kept separate from test/requirements.txt
# so neither CI job installs tooling it doesn't use. flake8 analyses source without
# importing it, so the runtime requirements are intentionally not included here.
darglint>=1.8,<2
dlint>=0.16,<1
flake8>=7.1,<8
flake8-annotations>=3.1,<4
flake8-blind-except>=0.2,<1
flake8-bugbear>=24.0
flake8-builtins>=2.5,<3
flake8-quotes>=3.4,<4
flake8-return>=1.2,<2
flake8-sfs>=1.0,<2
flake8-simplify>=0.21,<1
